### Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

### Network
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian13-template
d-i netcfg/get_domain string local

### Mirror
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### Account setup
# Root login disabled in favor of sudo, matching your manual notes.
d-i passwd/root-login boolean false
d-i passwd/user-fullname string Debian
d-i passwd/username string debian
d-i passwd/user-password password packer-temp-pw
d-i passwd/user-password-again password packer-temp-pw
d-i user-setup/allow-password-weak boolean true

### Clock and time zone
d-i clock-setup/utc boolean true
d-i time/zone string Etc/UTC
d-i clock-setup/ntp boolean true

### Partitioning - single ext4 root, NO swap (we run swapoff -a / no swap by design)
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select single-partition
d-i partman-auto/expert_recipe string \
      single-partition :: \
              1000 1 -1 ext4 \
                      $primary{ } $bootable{ } \
                      method{ format } format{ } \
                      use_filesystem{ } filesystem{ ext4 } \
                      mountpoint{ / } \
              .
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true
d-i partman-basicfilesystems/no_swap boolean false

### Base system / packages
tasksel tasksel/first multiselect standard, ssh-server
d-i pkgsel/include string sudo curl qemu-guest-agent
d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

# debian gives 'debian' the sudo group so we don't have to visudo by hand later
d-i passwd/user-default-groups string sudo

### Bootloader
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string default

### Finish
d-i finish-install/reboot_in_progress note
d-i cdrom-detect/eject boolean true
